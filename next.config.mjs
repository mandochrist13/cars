/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['i.pinimg.com','encrypted-tbn0.gstatic.com'],  // Remplacez par le domaine exact de votre image
    },
};

export default nextConfig;
